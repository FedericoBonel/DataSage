/** Renders the page that verifies a user account and provides feedback with next steps to start using the account. */
const Verification = () => {
  return (
    <div>Verification</div>
  )
}

export default Verification