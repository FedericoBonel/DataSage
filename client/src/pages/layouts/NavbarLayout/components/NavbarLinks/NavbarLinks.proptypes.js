import PropTypes from "prop-types";

export default {
    /** Optional. Styles to be applied to the links wrapper. (List component form MUI) */
    sx: PropTypes.any,
    /** Elements to be rendered in the list */
    children: PropTypes.node,
};
