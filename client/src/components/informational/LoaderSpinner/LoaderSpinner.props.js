import PropTypes from "prop-types";

export const propTypes = {
    /** Optional. Message to be shown with the spinner. */
    message: PropTypes.string,
    /** Optional. Its always true, if false, the spinner has a padding on its Y axis. */
    disablePadding: PropTypes.bool,
};