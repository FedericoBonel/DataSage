/** Contains all permissions validation values to be used in the application. */
export default Object.freeze({
    allowedActions: {
        /** Maximum length of an allowed action for a permission */
        MIN_LENGTH: 1,
        /** Maximum length of an allowed action for a permission */
        MAX_LENGTH: 255,
    },
});
