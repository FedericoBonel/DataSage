/** Contains all textsearch query validation values to be used in the application. */
export default Object.freeze({
    /** Minimum length of the text search query param */
    MIN_LENGTH: 1,
    /** Maximum length of the text search query param */
    MAX_LENGTH: 255,
});
