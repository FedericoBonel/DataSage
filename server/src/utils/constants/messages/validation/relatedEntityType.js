/** Contains all related entity types validation error messages to be used in the application. */
export default Object.freeze({
    name: {
        /** Message to be shown when a related entity type name is invalid. */
        INVALID: "You need to provide a related entity type name as a string.",
    },
});
