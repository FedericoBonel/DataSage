/** Contains all notification types validation error messages to be used in the application. */
export default Object.freeze({
    name: {
        /** Message to be shown when a notification type name is invalid. */
        INVALID: "You need to provide a notification type name as a string.",
    },
});
