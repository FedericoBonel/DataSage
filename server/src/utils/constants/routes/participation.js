/** Contains all routes for chat participation resources. */
export default Object.freeze({
    /** Route to the participation resource (used to accept invitations to a chat or exit chats). */
    PARTICIPATION: "participation",
});
