/** Contains all routes for notifications resources. */
export default Object.freeze({
    /** Route to the notifications resource. */
    NOTIFICATIONS: "notifications",
    /** Route to the not read notifications count resource. */
    NOT_READ: "notread",
});
